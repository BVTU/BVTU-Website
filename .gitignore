# System files
.DS_Store
Thumbs.db

# Database credentials — NEVER commit this
members/config.php

# Protected PDFs — kept on server only, not in public repo
members/protected-docs/

# Membership rosters — personal contact data. The repo root is the web root,
# so anything committed here is downloadable from the live site.
/*.xlsx
/*.csv
